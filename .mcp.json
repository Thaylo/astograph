{
  "mcpServers": {
    "astograph": {
      "command": "docker",
      "args": [
        "run", "--rm", "-i", "--pull", "always",
        "-v", ".:/workspace:ro",
        "-v", "./.metadata_astograph:/workspace/.metadata_astograph",
        "thaylo/astograph"
      ]
    }
  }
}
